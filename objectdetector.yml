ObjectDetector:
  Camera:
    Server: 1 # 0 -> Deactivate, 1 -> ICE, 2 -> ROS
    Proxy: "cameraA:tcp -h localhost -p 9999"
    Format: RGB8
    Topic: "/usb_cam/image_raw"
    Name: cameraA

Framework: TensorFlow  # Currently supported: "Keras" or "Tensorflow"

KerasModel: full_model  # path to model

TFModel:
  MODEL_NAME: ssd_mobilenet_v1_coco_2017_11_17  # Name of model, same as the tar.gz file present in Net/TensorFlow Folder
  CKPT: frozen_inference_graph.pb     # Frozen inference graph
  LABELS: mscoco_label_map.pbtxt  # Labels file
  NUM_CLASSES: 90

NodeName: dl-digitclassifier
